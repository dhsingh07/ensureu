package com.book.ensureu.notification.constant;

import com.book.ensureu.constant.ApplicationProfile;
import com.book.ensureu.constant.UserProfile;

public enum NotificationType {

	/*PROFILE_CREATE(EventType.CREATE, UserProfile.profileURL),
	PROFILE_UPDATE(EventType.UPDATE),
	PROFILE_FORGOTPASSWORD(EventType.UPDATE),
	PROFILE_VERIFICATION(EventType.VERIFICATION),
	PAPER_REMINDER(EventType.PAPERREMINDER);
	
	private final EventType eventType;
	private final static UserProfile userProfile;
	private final static ApplicationProfile applicationProfile;
	
	NotificationType(EventType eventType,UserProfile userProfile)
	{
		this.eventType=eventType;
	}
	
	NotificationType(EventType eventType,ApplicationProfile applicationProfile)
	{
		this.eventType=eventType;
	}*/
}
